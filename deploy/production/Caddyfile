{
	admin off
	email {$LAB_ACME_EMAIL}
}

{$LAB_PUBLIC_HOST} {
	encode zstd gzip
	reverse_proxy control-plane:8443

	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		X-Content-Type-Options "nosniff"
		Referrer-Policy "strict-origin-when-cross-origin"
		-Server
	}

	log {
		output stdout
		format json
	}
}
